No fixtures are required for the initial `git` skill Waza pilot.

If a future task needs sample diffs, commit messages, or repository state files,
add them here and document the mapping back to
`config/agents/skills/git/evals/evals.json`.
